import InfoPage from "@/components/InfoPage";

export default function ReturnsPage() {
  return (
    <InfoPage title="Cancellation & Returns">
      <p>Pending orders can be cancelled from the order details page.</p>
      <p>Return requests are available after delivery based on product condition and policy.</p>
    </InfoPage>
  );
}